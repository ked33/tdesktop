name: Build Windows x64 (Avrea)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: avrea-windows-2025-8-vcpu
    timeout-minutes: 720
    # GitHub-hosted (windows-2022) sccache via ghac. Disabled on Avrea:
    # meson wraps cl.exe with sccache while configuring liblcms2, then
    # sccache hits results-receiver.actions.githubusercontent.com and
    # gets HTTP 400 (invalid request body / ghac). Restore this env
    # block when switching runs-on back to windows-2022.
    # env:
    #   SCCACHE_GHA_ENABLED: "true"
    #   SCCACHE_IGNORE_SERVER_IO_ERROR: "1"
    #   SCCACHE_IDLE_TIMEOUT: "0"

    steps:
      - name: Prepare directories
        shell: pwsh
        run: |
          $root = Join-Path $env:GITHUB_WORKSPACE 'TBuild'
          New-Item -ItemType Directory -Force -Path $root | Out-Null
          New-Item -ItemType Directory -Force -Path (Join-Path $root 'ThirdParty') | Out-Null
          New-Item -ItemType Directory -Force -Path (Join-Path $root 'Libraries') | Out-Null
          New-Item -ItemType Directory -Force -Path (Join-Path $root 'Libraries\win64') | Out-Null
          "TBUILD=$root" >> $env:GITHUB_ENV
          "TBUILD_REL=TBuild" >> $env:GITHUB_ENV
          "LibrariesPath=$(Join-Path $root 'Libraries\win64')" >> $env:GITHUB_ENV

      - name: Get repository name
        shell: bash
        run: echo "REPO_NAME=${GITHUB_REPOSITORY##*/}" >> $GITHUB_ENV

      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          submodules: recursive
          path: ${{ env.TBUILD_REL }}/${{ env.REPO_NAME }}

      - name: Set up Python 3.10
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Compute cache key
        shell: bash
        run: |
          PREPARE_ROOT="$GITHUB_WORKSPACE/$TBUILD_REL/$REPO_NAME/Telegram/build/prepare"
          DOC_PATH="$GITHUB_WORKSPACE/$TBUILD_REL/$REPO_NAME/docs/building-win.md"
          {
            find "$PREPARE_ROOT" -type f -print0 | sort -z | xargs -0 sha256sum
            if [ -f "$DOC_PATH" ]; then
              sha256sum "$DOC_PATH"
            fi
          } > CACHE_KEY.txt
          echo "CACHE_KEY=$(sha256sum CACHE_KEY.txt | awk '{ print $1 }')" >> $GITHUB_ENV

      - name: Restore third-party cache
        id: cache_third_party
        uses: actions/cache/restore@v6
        with:
          path: ${{ env.TBUILD }}\ThirdParty
          key: win64-third-party-${{ env.CACHE_KEY }}
          restore-keys: |
            win64-third-party-

      - name: Restore libraries cache
        id: cache_libraries
        uses: actions/cache/restore@v6
        with:
          path: |
            ${{ env.LibrariesPath }}\*
            !${{ env.LibrariesPath }}\cache_keys
            !${{ env.LibrariesPath }}\[qQ]t[_-]*
            ${{ env.LibrariesPath }}\cache_keys\*
            !${{ env.LibrariesPath }}\cache_keys\[qQ]t[_-]*
          key: win64-libs-release-${{ env.CACHE_KEY }}
          restore-keys: |
            win64-libs-release-
            win64-libs-${{ env.CACHE_KEY }}

      - name: Restore Qt cache
        id: cache_qt
        uses: actions/cache/restore@v6
        with:
          path: |
            ${{ env.LibrariesPath }}\[qQ]t[_-]*
            ${{ env.LibrariesPath }}\cache_keys\[qQ]t[_-]*
          key: win64-qt-release-${{ env.CACHE_KEY }}
          restore-keys: |
            win64-qt-release-
            win64-qt-${{ env.CACHE_KEY }}

      # GitHub-hosted (windows-2022): puts sccache.exe on PATH and uses
      # the ghac backend. On Avrea that PATH wrap makes meson treat
      # "sccache.exe cl" as the compiler and fail as above. Restore this
      # step (and the job env / CMAKE_*_COMPILER_LAUNCHER flags) when
      # switching runs-on back to windows-2022.
      # - name: Set up sccache
      #   uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696
      #   with:
      #     version: "v0.15.0"

      - name: Configure MSVC environment
        shell: cmd
        run: |
          set "VSWHERE=%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe"
          if not exist "%VSWHERE%" exit /b 1
          for /f "usebackq delims=" %%i in (`"%VSWHERE%" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "VSINSTALLPATH=%%i"
          if not defined VSINSTALLPATH exit /b 1
          rem Avrea / VS 2026 default toolset is 14.51 (v145). libvpx
          rem --target=x86_64-win64-vs17 then fails with
          rem "Toolchain is unable to link executables". Pin 14.44
          rem (Yurigram + docs/building-win.md). On windows-2022 the
          rem unpinned VsDevCmd line below is enough if that image is VS 2022.
          call "%VSINSTALLPATH%\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 -vcvars_ver=14.44
          rem call "%VSINSTALLPATH%\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64
          echo Platform=x64>>"%GITHUB_ENV%"
          for /f "delims=" %%i in ('set') do @echo %%i>>"%GITHUB_ENV%"

      - name: Check supplemental search progress, merging and limits
        shell: cmd
        run: |
          cl /nologo /std:c++20 /EHsc /W4 /WX ^
            /I"%TBUILD%\%REPO_NAME%\Telegram\SourceFiles" ^
            "%TBUILD%\%REPO_NAME%\Telegram\SourceFiles\test\test_porn_search.cpp" ^
            /Fe:"%RUNNER_TEMP%\test_porn_search.exe" ^
            /Fo:"%RUNNER_TEMP%\test_porn_search.obj"
          if errorlevel 1 exit /b %errorlevel%
          "%RUNNER_TEMP%\test_porn_search.exe"
          exit /b %errorlevel%

      - name: NuGet sources
        shell: cmd
        run: |
          nuget sources Disable -Name "Microsoft Visual Studio Offline Packages"
          nuget sources Add -Source https://api.nuget.org/v3/index.json & exit 0

      - name: Prepare third-party libraries
        id: prepare_libraries
        shell: cmd
        run: |
          rem Avrea: keep ghac/sccache launcher off (see job comments).
          rem Do not set CC=cl / CXX=cl: libvpx's bash configure
          rem (--target=x86_64-win64-vs17) then fails with
          rem "Toolchain is unable to link executables".
          rem Drop these unsets when switching runs-on back to windows-2022
          rem and restoring sccache-action / SCCACHE_GHA_ENABLED.
          set SCCACHE_GHA_ENABLED=
          rem set CC=cl
          rem set CXX=cl
          set CMAKE_C_COMPILER_LAUNCHER=
          set CMAKE_CXX_COMPILER_LAUNCHER=
          cd /d %TBUILD%
          rem Avrea BuildTools often has no ATL (atlbase.h). dump_syms needs
          rem it; this CI only builds Release Telegram with NO_PDB. Yurigram
          rem uses the same flags. Drop skip-dump-syms on windows-2022 if
          rem you need crash symbols. skip-debug skips Debug library builds.
          %REPO_NAME%\Telegram\build\prepare\win.bat skip-debug skip-dump-syms silent

      - name: Save third-party cache
        if: steps.prepare_libraries.outcome == 'success' && steps.cache_third_party.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        continue-on-error: true
        with:
          path: ${{ env.TBUILD }}\ThirdParty
          key: ${{ steps.cache_third_party.outputs.cache-primary-key }}

      - name: Save libraries cache
        if: steps.prepare_libraries.outcome == 'success' && steps.cache_libraries.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        continue-on-error: true
        with:
          path: |
            ${{ env.LibrariesPath }}\*
            !${{ env.LibrariesPath }}\cache_keys
            !${{ env.LibrariesPath }}\[qQ]t[_-]*
            ${{ env.LibrariesPath }}\cache_keys\*
            !${{ env.LibrariesPath }}\cache_keys\[qQ]t[_-]*
          key: ${{ steps.cache_libraries.outputs.cache-primary-key }}

      - name: Save Qt cache
        if: steps.prepare_libraries.outcome == 'success' && steps.cache_qt.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        continue-on-error: true
        with:
          path: |
            ${{ env.LibrariesPath }}\[qQ]t[_-]*
            ${{ env.LibrariesPath }}\cache_keys\[qQ]t[_-]*
          key: ${{ steps.cache_qt.outputs.cache-primary-key }}

      - name: Configure project
        shell: cmd
        env:
          TDESKTOP_API_ID: ${{ secrets.TDESKTOP_API_ID }}
          TDESKTOP_API_HASH: ${{ secrets.TDESKTOP_API_HASH }}
        run: |
          if "%TDESKTOP_API_ID%"=="" exit /b 1
          if "%TDESKTOP_API_HASH%"=="" exit /b 1
          cd /d %TBUILD%\%REPO_NAME%\Telegram
          configure.bat -G "Ninja Multi-Config" ^
            -D TDESKTOP_API_ID=%TDESKTOP_API_ID% ^
            -D TDESKTOP_API_HASH=%TDESKTOP_API_HASH% ^
            -DCMAKE_CONFIGURATION_TYPES=Release ^
            -D DESKTOP_APP_NO_PDB=ON ^
            -D CMAKE_COMPILE_WARNING_AS_ERROR=OFF ^
            -D CMAKE_MSVC_DEBUG_INFORMATION_FORMAT=
            rem GitHub-hosted (windows-2022): restore these two with sccache-action:
            rem -D CMAKE_C_COMPILER_LAUNCHER=sccache
            rem -D CMAKE_CXX_COMPILER_LAUNCHER=sccache

      - name: Build Telegram (Release)
        shell: cmd
        run: |
          cd /d %TBUILD%\%REPO_NAME%\out
          cmake --build . --config Release --target Telegram --parallel

      # GitHub-hosted (windows-2022): restore with the sccache-action step.
      # - name: Show sccache statistics
      #   if: always()
      #   shell: pwsh
      #   continue-on-error: true
      #   run: |
      #     $sccache = $env:SCCACHE_PATH
      #     if (-not $sccache) {
      #       $cmd = Get-Command sccache -ErrorAction SilentlyContinue
      #       if ($cmd) { $sccache = $cmd.Source }
      #     }
      #     if (-not $sccache) {
      #       Write-Host "sccache not available; skipping statistics"
      #       exit 0
      #     }
      #     & $sccache --show-stats
      #     exit 0

      - name: Package artifact
        shell: pwsh
        run: |
          $exe = "$env:TBUILD\$env:REPO_NAME\out\Release\Telegram.exe"
          if (-not (Test-Path -LiteralPath $exe)) { exit 1 }
          $artifactDir = Join-Path $env:GITHUB_WORKSPACE 'artifact'
          New-Item -ItemType Directory -Force -Path $artifactDir | Out-Null
          Copy-Item -LiteralPath $exe -Destination (Join-Path $artifactDir 'Telegram.exe')

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: Telegram-x64
          path: artifact/
          if-no-files-found: error
