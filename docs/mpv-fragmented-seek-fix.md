# MPV 分片 MP4 的缓存外跳转

当前状态：`cb851f26e9` 的双端日志已确认立即回弹的机制。当前实现让无法在旧视图完成的回跳立即进入新视图加载，等待索引后再恢复播放，并处理重复目标、加载期间的新目标和暂停状态。完整客户端与真实 Telegram 网络的验证边界见文末。

没有全局索引的分片 MP4 按目标时间准备跳转入口，避免每次首次远跳都等待全文件元数据扫描。本次修复进一步保留缓存外反向跳转的原始目标，避免 MPV 已返回旧分片起点后被误判为跳转成功。首播继续使用快速打开选项，普通 MP4 的 Range、大文件头和多 `mdat` 支持保持有效。

## cb851f26e9 的根因与本轮修复

这次日志能区分“目标丢失”和“目标仍在，但旧视图恢复播放”。对于 166.583561 秒的回跳，控制器始终保留原目标；当前 URL 把此前分片包装为 `free` atom，MPV 无法从这个视图取得目标处的帧，却在新索引完成前恢复了旧视图的 429.6 秒位置。仅保留目标、检查最终是否到达，无法阻止这个中途回弹。

| 证据 | 结论 |
|---|---|
| `mpv - cb851f26e9.log` 第 1959–1963 行请求并执行 166.583561 秒，第 2011 行恢复到 429.6 秒 | 从输入到错误恢复仅 84 ms，小于旧控制器的 200 ms 原生跳转宽限 |
| `log - cb851f26e9.txt` 第 351–353 行记录 revision 2、`target` 与 `logged_target` 都是 166.583561 | 这次原始目标没有丢失，索引正在准备 |
| Telegram 日志第 406–409 行为同一 166583 ms 目标创建 revision 3，并取消 revision 2 | 重复输入会重建相同索引，使等待继续延长 |
| Telegram 日志第 591–611 行：349.223368 秒视图已打开但仍 `reloading=true`，新的 606.123317 秒目标被保留 | 旧重载状态阻止为新目标准备索引 |
| MPV 日志第 4724、5062 行：606.123317 秒输入后约 26.5 秒才恢复 | 单纯保留目标不足以保证新目标及时接管 |

HTTP 日志中还存在被替代的请求及并发预览读取。这些会影响耗时；上述证据没有排除全部网络或调度问题，本轮修改针对已确认的控制器决策链。

实现保留以下边界：

1. 索引结果同时报告新视图保留的最早分片时间。只有已确认目标早于当前视图起点时，才跳过 200 ms 宽限，立即用目标 revision 重新打开；视图内的正常缓存跳转仍优先交给 MPV。
2. 普通与特殊媒体 HTTP 桥在发送响应前等待该 revision 的索引快照。等待期间检查连接关闭、取消和 revision 过期；完整索引回退成功时可继续读取。MPV 因此处于正常加载状态，期间 `time-pos` 可以为 null，而不是继续报告并播放错误旧位置。
3. 同一目标复用正在准备的 revision；目标保留到实际完成。已发送的重载按目标序号与 revision 去重，防止 playback-restart 到位置确认回复之间再次加载同一目标。
4. 等待自身索引视图时，接管可解析的绝对、百分比和相对 seek 输入，相对输入以等待中的目标为基点。此时即使 MPV 原生命令返回 `error running command`，新目标也能替换旧目标。普通播放中的命令仍关联执行日志或位置属性；未知格式不会被猜成目标。
5. 区分重载命令待回复、索引视图加载中以及启动 seek 尚未结束。新的已执行目标可以在旧重载恢复前准备索引，旧查询不能覆盖加载中的新目标。
6. 按需重载不再使用文件级 `pause` 选项。该选项可能在异步加载后期应用，并在替换文件时恢复备份值，从而覆盖用户刚点击的继续播放。现在保留 MPV 的实时全局暂停状态，仅在需要恢复 EOF 自动暂停时显式更新它。
7. 等待期间的显式 `stop` / `quit` 立即取消控制器并恢复原 idle 设置，避免把用户停止误当成内部换视图产生的停止事件而继续等待。

本机对照直接使用生产 `PreparedIndex` / `IndexReload` 与真实 MPV，索引 Reader 用本地文件模拟 400 ms 冷块延迟，HTTP 媒体读取模拟 75 ms 延迟。本轮不延迟 IPC 接收、不丢弃位置通知。回归标准增加了整个跳转过程的进度采样与属性事件检查，不能只看最终落点。

| 503.764744 → 218.766363 秒对照 | 结果 |
|---|---|
| 诊断版本，`live-pending-baseline-v1/result.json` | 最终可以到达目标，但期间有 126 个旧进度采样，首次为 499.2 秒，且已经恢复播放；按新标准失败 |
| 当前修复，`live-pending-back-v3/result.json` | 到达目标并继续播放，中途旧进度采样为 0 |

已完成的实际 MPV 回归覆盖等待期间重复 exact/keyframes、连续更换目标、旧重载尚未恢复时的新目标、相对跳转、暂停与继续、缓存内关键帧跳转、切换文件和关闭播放器。`live-pending-relative-v1` 曾发现暂停状态被晚到的文件选项覆盖；修正后 `live-pending-relative-v2` 保持继续播放并到达最后的 298.266363 秒目标。`live-pending-reload-last-v3` 在新输入后约 0.73 秒开始为 606.123317 秒准备索引。

九种短夹具布局共 45 次跳转通过，结果为 `layouts-matrix-pending-v2.json`。这些用例检查普通 MP4、大 `moov`、多 `mdat`、尾部 `moov`、全局分片索引、无全局索引及 B 帧布局的正常跳转和音视频对齐；它们不能代替大样本的冷回跳对照。独立 MP4 索引测试通过 535,068 项检查，并验证视图起点、时间偏移、读取预算和原字节偏移。

控制器的 42 组确定性回归、4,108 项检查通过独立 MSVC Debug 编译和运行，覆盖缺失通知、未知日志、重复目标、新目标接管、暂停变化、EOF 自动暂停、延迟位置确认及停止命令。新增的“延迟位置确认”用例在加入重载去重前稳定失败，错误为 `a delayed completion reply cannot reload the same target twice`。正常 EOF 退出与 `idle=yes,keep-open=always` 的恢复也通过了实际 MPV 验证；EOF 检查在等待进程退出前关闭测试观察连接，避免无人消费的位置事件阻塞测试端的管道关闭。等待中的停止与关闭在 `live-pending-stop-v4`、`live-pending-close-v4` 中均正常释放 Reader，驱动退出码为 0。

## 早期原因

用户提供的 `log - db21ec8.txt` 与 `mpv - db21ec8.log` 表明，后台元数据扫描仍明显落后于播放器读取。上一版虽然恢复了首播，却依然需要扫描全部分片后才能可靠跳到未缓存位置。Telegram 按块下载，因此只保存少量元数据也可能需要读取分散在整个文件中的许多远程块。

样本 `100.mp4` 为 310,189,901 字节，包含 338 个 `moof` 和 338 个 `mdat`，没有 `sidx` 或 `mfra` 全局索引。音频和视频通常分开存放在相邻分片中，文件开头还有空轨道分片。

## 实现

1. 初始播放继续使用 `ignore_editlist=1,fflags=+ignidx`。独立 Reader 读取轨道信息与少量初始分片，保留各轨的时基、默认采样参数和分片位置。
2. 缓存外跳转时，利用目标时间和已知文件位置估算探测点，读取附近的 `moof`，解析 `tfdt`、`tfhd`、`trun`。视频定位考虑首帧的呈现时间偏移，选择可独立解码的分片，并同时定位音频。
3. 每个跳转结果使用独立的 `?tdesktop_index=<revision>` URL。在这一固定视图中，把第一个 `moof` 的前 16 字节输出为一个 64 位长度的 `free` atom，让解复用器直接跳到所有轨道所需的最早分片。其余媒体字节、文件总长度和媒体偏移都保持原值，磁盘原文件不变。
4. MPV 在同一进程重新打开这一视图，仍使用快速打开选项，从目标附近建立局部采样索引并恢复准确时间和暂停状态。后续分片可以继续顺序读取，不需要补造全局索引。
5. 新目标可以取代尚未完成的定位；已经正常完成的缓存内跳转会取消多余工作。最多保留 8 份跳转视图，正在发送的 HTTP 响应持有自己的固定快照。
6. 首次按需重载前临时启用 MPV 的 idle，避免等待定位时播放器因 EOF 退出。结束控制或切换文件时恢复原值；用户的 `keep-open` 设置不变。暂停状态区分用户操作和 EOF 自动暂停。
7. 已有全局索引、未知时长或无法可靠解析的布局继续使用完整元数据缓存作为回退。回退仍有扫描成本；关闭播放器或切换文件会取消后台工作，Reader 在主线程停止并释放。

解析限制包括：最多 8 个音视频轨道、4 MiB `moov`、256 KiB `moof`、每轨分片合计 65,536 个采样、每次读取 64 KiB、单个探测窗口 4 MiB、每次定位读取预算 16 MiB、48 轮定位和 8,192 个缓存定位点。未知标志、无效字段和越界时间不会生成跳转入口。

## 前期布局与索引验证

以下记录布局、解析器和早期 IPC 回归。这些结果不能单独证明 MPV 合并位置通知时仍能保留反向目标；本次有效对照见“反向跳转回归”。

2026-09-13 使用真实 MPV、局部 HTTP 服务以及独立 MSVC Debug 构建进行验证。音视频输出使用 null 后端，检查实际播放时间、音频时钟和 seek 状态。暂停的位置必须稳定至少 300 ms；音频状态在恢复解码后检查，避免将暂停时尚未启动的音频输出误判为失败。

以下 10 类输入均通过首播、5 次单次跳转、连续 6 次跳转的最终位置，以及恢复播放和音频对齐检查。每个首次读取冷块模拟 100 ms 延迟，MPV 缓存关闭。

| 输入布局 | 结果 |
|---|---|
| 普通前置 `moov` | 通过 |
| 3 MiB 大 `moov` | 通过 |
| 多 `mdat` | 通过 |
| 大 `moov` 与多 `mdat` | 通过 |
| 尾部 `moov` | 通过 |
| 含 `mfra` 的分片 MP4 | 通过 |
| 含 `sidx` 的分片 MP4 | 通过 |
| 无全局索引的生成分片 MP4 | 通过 |
| 含 B 帧的无索引分片 MP4 | 通过 |
| 用户样本 `100.mp4` | 通过 |

在上述布局测试中，用户样本约 3.18 秒首播，5 次跳转约 3.73–4.28 秒。脚本中的定位调用使用实际 C++ 解析器，HTTP 服务和重载调度由 Python 驱动。

另外，以真实 C++ `PreparedIndex` 和 IPC 控制器驱动 MPV，替身 Reader 的冷块延迟设为 400 ms，媒体服务设为 75 ms。用户样本在暂停和正常播放中均完成 760、803、127、337、718 秒的连续跳转；暂停场景首播约 3.44 秒，各次跳转约 4.45、4.80、4.84、11.02、3.75 秒，随后继续跨分片播放。该测试还覆盖：

- 快速连续拖动、定位期间更换目标，以及重载完成后再次跳转。
- 等待跳转时暂停或继续播放，保持用户的新状态。
- 缓存内跳转不触发重载，以及关键帧跳转。
- 已重载后切换文件、定位中关闭播放器和 Reader 主线程清理。
- 默认配置下正常播放到结尾退出，以及恢复用户的 `idle=yes,keep-open=always` 设置。

早期实现无法接管 MPV 原生打开文件时返回 `error running command` 的 seek。本轮针对自身索引视图的等待阶段补上可解析目标的接管；其他打开阶段或无法解析的输入仍保留原生语义。

独立 MP4 文件头和索引测试、Smart 策略公式及 56 项结构检查通过。索引测试包含前缀边界、原偏移保持、空轨道分片、呈现时间偏移、取消、无效输入、时间溢出和读取/采样预算。索引测试已接入现有 Windows 构建检查。

## 反向跳转回归（2026-09-13）

### 日志证据与原因

`a84a5a0` 与 `f107726` 的匹配播放记录出现同一种错误：MPV 接受较早的时间目标，但当前按需视图从较晚分片开始，解复用器很快返回这一视图的最早帧。播放恢复不等于到达了用户目标。

| 日志 | 原始目标与字节定位 | 实际结果 |
|---|---|---|
| `mpv - a84a5a0.log` | 第 1048 行请求 572.003793 秒；第 1117 行按需重载 | 第 1476 行恢复到 572.000000 秒，前进成功 |
| `mpv - a84a5a0.log` | 第 1733 行请求 319.117906 秒；第 1747 行仍读取字节 212927457 | 第 1787 行恢复到 570.000000 秒，没有为原目标重载 |
| `mpv - a84a5a0.log` | 第 4419 行请求 708.481891 秒；第 4480 行按需重载 | 第 4733 行恢复到 708.480000 秒，前进成功 |
| `mpv - a84a5a0.log` | 第 5491、9649 行请求 351.230399、132.464036 秒；均读取字节 262475002 附近 | 第 5540、9706 行均恢复到 704.400000 秒 |
| `mpv - f107726.log` | 第 2638 行请求 114.400759 秒；第 2652 行仍读取字节 212927457 | 第 2692 行恢复到 570.000000 秒；随后请求 158、202、303 秒也得到相同落点 |

`log - a84a5a0.txt` 仅第 153、488 行完成 572、708 秒的索引准备；第 1059 行累计 `http_failed=0`。`log - f107726.txt` 包含两轮播放，这份 MPV 日志对应第 1450 行、11:47:13 开始的第二轮；不能把第一轮的反向索引记录混入对照。第二轮仅第 1490、1587 行完成 309、572 秒的准备，第 1899 行同样为 `http_failed=0`。这些证据指向时间目标丢失，没有显示反向 Range 下载失败；日志未记录原始 HTTP Range 响应头。

两份 MPV 日志版本均为 `v0.41.0-dev-g41f6a6450`，启用 `hr-seek=yes` 和 seekable cache；运行中的网络配置把前向缓存设为 256 MiB、后向缓存设为 128 MiB，预读 10 秒、滞回 5 秒。用户操作通常是暂停、发送 `absolute+exact`、约 50–80 ms 后继续播放。`f107726` 第 5043、5047 行还连续发送了 305.068690 秒 exact 和 303.061659 秒 keyframes，最终目标已经变化；只读取 exact 的 `hr-seek` 日志会漏掉后一次输入。

前版订阅 `time-pos`，保存 seek 后、playback-restart 前的首份属性快照，修正了查询响应来得太晚的一种情况。但 `observe_property` 通知可以合并，中间的原目标快照不一定送达。此时快照和随后查询都可能只剩 570 或 704.4 秒的错误落点，控制器把它当作用户目标并提前结束定位。仅延迟处理已排队的通知不能证明这一缺失场景已覆盖。

### 目标保留与兼容路径

按需索引可用时，控制器通过现有 IPC 订阅 debug 级日志，仅处理 `cplayer` 来源的已识别格式：

1. 普通播放时，`Run command: seek` 保留可解析的绝对目标，识别 exact、keyframes 和绝对百分比；连续命令更新目标及序号。命令日志本身不代表 MPV 已受理或执行，仍需执行证据。自身索引视图等待期间的接管例外见本轮修复。
2. 与当前操作关联的 `hr-seek, skipping to` 补充实际执行的准确目标。关键帧跳转可以没有这条日志，因此还关联 `playback restart complete @` 的实际落点，包含暂停状态后缀。当视图最早帧明显晚于请求目标时保留原目标；合法的原生关键帧落点和已完成的缓存内跳转继续使用原有完成判定。
3. 已确认的日志目标不会被较晚的属性响应覆盖；完成后作废在途的位置查询。重载前检查文件所有权及目标序号，连续拖动、更换文件、停止或退出不会让旧目标重新加载到新操作之上。
4. 原有 `time-pos` 订阅、异步查询、seek 和 playback-restart 路径仍然保留。普通相对定位、属性定位和其他无法从命令可靠还原的操作继续使用执行日志或属性回退；帧、字幕等非普通 seek 操作只在能够关联执行日志时补充目标。

核心日志文本不是 MPV 的稳定 API。未知格式、未知参数或无效数值不会被猜成目标；缺少可用日志时继续走属性路径，这种兼容回退不能保证修复所有未来版本的通知合并情形。实现没有改变用户的 MPV 配置文件。

上版对大 `mdat` 探测窗口的修复仍保留：插值点和搜索上界落在同一个媒体块时，从最后确认的分片边界继续读取元数据，仍遵守读取预算和迭代上限。

### 有效对照与防回归

本次对照使用真实 C++ `PreparedIndex` / `IndexReload`、真实 MPV 和本机 HTTP 服务。索引 Reader 的冷块延迟为 400 ms，媒体服务为 75 ms；输入沿用用户的暂停、seek、恢复顺序。测试替身以单个 pending 标记将 IPC 接收合并延后 350 ms，并丢弃所有 `time-pos` 属性通知，保留查询响应、seek/restart 和日志消息。基线也订阅相同级别日志，使消息量一致。这是受控缺失通知和接收延迟实验，不是 MPV 自然复现概率的证明。此前仅使用 `singleShot` 延迟接收、仍保留目标通知的实验不作为有效基线。

| 对照 | 结果 |
|---|---|
| `live-playuserproofbaseline1/result.json` | 572.003793 秒成功；回到 319.117906 秒超时，仍播放约 586.32 秒；控制器错误地为 570 秒附近的落点准备索引 |
| `live-playuserprooffixed1/result.json` | 572.003793 → 319.117906 → 708.481891 → 351.230399 → 132.464089 秒五次全部通过，音视频同步并继续播放；C++ 索引请求保持这五个目标 |

修复组五次耗时约 6.60、17.69、5.73、11.38、14.87 秒，仅反映上述模拟延迟。独立 IPC 测试 `test_mpv_index_reload` 的 30 组测试均已通过 MSVC Debug 编译并运行，覆盖缺失快照、日志和属性顺序变化、最后目标替换、暂停恢复、查询失效及未知格式回退。字幕跳转执行前穿插无关音量日志的定向用例，在修正前失败、修正后通过。

保持暂停的补充对照 `live-sample-paused-final1/result.json` 使用相同的通知缺失与 350 ms 接收延迟，572.003793 → 319.117906 秒两次跳转均成功，分别触发目标索引视图，暂停状态保留。恢复解码时音视频位置一致，最后恢复播放后进度从约 320.36 秒继续到 325.40 秒，驱动退出码为 0。

最终源码对应的驱动另完成以下四项实际 MPV 回归，命令均返回 success，Python 与驱动退出码均为 0。冷反向和末次目标用例沿用上述通知缺失及 350 ms 接收延迟；另外两项不注入延迟或丢失通知。

| 回归记录 | 结果 |
|---|---|
| `live-agent-final-cached-v1/result.json` | 缓存内定位到 1 秒，无索引请求，URL 未重载，暂停与恢复解码正常 |
| `live-agent-final-cold-keyframe-v1/result.json` | 572.003793 → 114.400759 秒关键帧回跳落到 114.4 秒，索引请求保持原目标，未停在 570 秒 |
| `live-agent-final-last-keyframe-v1/result.json` | 305.068690 秒 exact 后 71 ms 发送 303.061659 秒 keyframes，落到 303.08 秒，只为末次目标准备索引，音视频同步 |
| `live-agent-final-switch-v1/result.json` | 760 秒按需重载完成后切换到本地文件，旧控制器释放、新文件保持，默认 idle 与暂停状态恢复正确 |

另外使用独立 C++ 驱动快照，IPC 接收不加延迟、不丢属性通知，对 regular、large-moov、multiple-mdat、large-moov-multiple-mdat、tail-moov、fragmented-mfra、fragmented-sidx、fragmented-no-index、fragmented-b-frames 九种布局执行 36 → 6 → 38 → 16 → 34 秒跳转。45 次均通过暂停定位及恢复后的音视频检查，退出码均为 0，结果记录在 `layouts-matrix-v1.json` 及各 `live-layouts-<布局>-v1/result.json`。

这些短夹具的原生 seek 均能完成，路径保持原 URL，没有生成额外索引视图或误触发重载；无索引与 B 帧夹具运行了 C++ 按需索引初始化。这组结果验证正常布局和缓存跳转未被破坏，不能代替用户大样本的冷索引反向重载对照。

## 788b38b 的回弹反馈与诊断补充

用户反馈的问题仍存在：从未缓存的较早时间跳转后，进度立即回弹到之前的位置。新日志第 1583、1586 行记录 218.766363 秒的命令和执行目标，第 1641 行却在 499.2 秒恢复；第 3373、3376 行请求 405.420232 秒，第 3420 行在 734.4 秒恢复。安装程序包含上一版新增逻辑，IPC 连接从启动保持到退出。

此前控制器测试和实际 MPV 回归主要检查最终到达目标、音视频同步及资源清理，没有将中途回弹到旧位置作为失败条件。因此这些通过结果不足以证明用户描述的问题已经解决。

新的本机采样没有注入 IPC 延迟或丢弃位置通知，也没有强制提高 cplayer 的命令行日志等级。503.764744 → 218.766363 秒回跳时，位置先报告 218.766363 秒，约 80 ms 后报告 499.32 秒，随后数秒仍播放旧位置。控制器诊断同时显示目标一直保留为 218.766363 秒，索引 revision 2 准备好后才重载。完整样本见 `live-playuser-diagnostics-positions1/position-samples.jsonl` 和 `diagnostic-verification.json`。这个替身 Reader 实验确认了中途回弹，但不能证明真实 Telegram 的索引请求具有相同的完成或取消过程。

本次补充沿用现有播放日志开关，在创建控制器时接入运行诊断。它记录关键状态转换，不记录正常播放时的每次位置更新：

- `MPV controller` 后的 JSON 记录日志订阅请求与响应、输入目标、执行目标、查询响应、目标接受或保护、完成判定和重载响应。`target`、`logged_target`、`candidate`、`value` 使用秒；`serial` 与 `revision` 用于关联同一次操作。
- `index_state` 对应 Unknown=0、Preparing=1、Ready=2、OnDemand=3、Unavailable=4。加载、暂停、重载和查询状态用于解释未发起重载的条件。
- `MPV index requested / seek started / seek cancelled / settled` 补齐准备成功之前的过程。取消记录包含原目标、原 revision、当前 revision，以及读取量和耗时；发布前被替换的结果也会记录。

诊断补充已通过独立 MSVC Debug 编译，原有 30 组控制器测试通过；实际 MPV 输出了完整的订阅、目标、错误落点、索引准备和重载记录。另一次连续输入 760 → 127 秒的测试记录了 revision 1 在准备期间被 revision 2 取消、后者完成并到达 127 秒，结果保存在 `live-reloadburst-diagnostics-cancel-early1/`。该变更用于定位真实客户端的决策链，尚未修复中途回弹行为。

## 时长与验证边界

用户样本声明时长约 844.98 秒，但逐包检查得到最后的视频帧为 834.12 秒，最后音频包约为 834.15 秒。842 秒处没有实际帧。定位可以识别已到达物理文件尾并选择最后分片，避免因声明时长偏大而退回全量扫描；这不会补出不存在的内容。正常退出测试从 831 秒播放到真实结尾。

以上延迟来自模拟冷块，不代表用户网络中的耗时。无法可靠解析的布局仍需要完整扫描，不能保证所有文件都能立即跳转。

本机没有配置好的 Telegram 完整构建树及项目依赖，未编译完整客户端，也未运行真实 Telegram Reader / MTProto 远程播放。独立 Qt 驱动和本机 HTTP 验证不能替代完整客户端的 Actions 构建、真实网络验证或可视/听感检查。

## 复现

本次 IPC 控制器回归需要独立测试目标 `test_mpv_index_reload` 与真实 C++ / MPV 驱动。下面的现有脚本由 Python 调度重载，用于布局和解析器验证，不能单独覆盖通知合并问题。先准备 `test_mp4_header`、`test_mp4_index`，再从仓库根目录运行：

```bash
python Telegram/SourceFiles/test/test_mpv_seek.py \
  --mpv "D:/D-Software/mpv-hero/mpv.exe" \
  --ffmpeg "D:/D-Software/yt-dlp/ffmpeg.exe" \
  --header-test "out/Debug/test_mp4_header.exe" \
  --index-test "out/Debug/test_mp4_index.exe" \
  --sample "D:/D-Download/100.mp4" \
  --latency-ms 100
```

脚本生成布局样本，并记录 MPV 日志、HTTP 请求、冷块读取和 `summary.json`。`--skip-fixtures` 只检查指定样本；`--previous-behavior` 用首播前完整扫描索引的选项作对照。回环测试单独清除子进程代理环境，不修改用户配置。
